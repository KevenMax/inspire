<p id="notice"><%= notice %></p>

<h1>Listing Empresas</h1>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>Nome:</th>
      <th>Endereço:</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @empresas.each do |empresa| %>
      <tr>
        <td><%= empresa.nome %></td>
        <td><%= empresa.endereco %></td>
        <td><%= link_to 'Visualizar', empresa %></td>
        <td><%= link_to 'Editar', edit_empresa_path(empresa) %></td>
        <td><%= link_to 'Apagar', empresa, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Enviar CSV', controller: 'csv', action: 'index' %>
<%= link_to 'New Empresa', new_empresa_path %>
